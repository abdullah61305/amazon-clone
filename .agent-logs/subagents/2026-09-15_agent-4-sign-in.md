# Agent 4 — client-side sign-in

- Scope: browser-only sign-in / create-account flow in the header account menu.
- Files owned: `lib/account.ts` (new), `components/header/sign-in-dialog.tsx` (new), `components/header/account-menu.tsx`.

## Log

- 2026-09-15T13:46Z Inspected `components/header/account-menu.tsx` (hover flyout with disabled Sign in button + note), `lib/location.ts` (existing `useSyncExternalStore` + localStorage + custom event pattern, reused its shape), `components/ui.tsx` `Logo` (hardcodes `text-white`, so a dark wordmark is rendered inline in the dialog), `app/globals.css` for `field`, `btn-yellow`, `btn-white`, animation durations (fade-in 150ms, rise 200ms).
- 2026-09-15T13:47Z Created `lib/account.ts`: `useAccount()` over key `amzn-clone:account`, snapshot cached by raw string so `useSyncExternalStore` gets a stable reference; server snapshot `null`; `signIn(email, name?)` / `signOut()` write storage in try/catch and dispatch `amzn-clone:account-change`; other tabs update through the `storage` event. `nameFromEmail` splits the local part on `. _ - +`, strips digits, capitalises the first non-empty segment, falls back to "there". Only email + name are stored; the password never reaches this module.
- 2026-09-15T13:49Z Created `components/header/sign-in-dialog.tsx`: portal to `document.body` (avoids header stacking context), `role="dialog" aria-modal`, labelled by the heading, autofocus first field, Escape / backdrop / close button dismiss, Tab focus trap, body scroll lock, full-height sheet on phones and 400px card from `sm`. Sign-in and create modes (create adds name; typed first name is used). Validation derived from state after the first submit, so errors update live without effects. Email-or-10+-digit-phone and min-6 password rules with Amazon wording, `text-deal` + red border, `aria-invalid`/`aria-describedby`. Submit disables the button, shows a spinner for 500ms, then `signIn` and close. Grey safety line under the password field. Password lives only in component state and is discarded on unmount.
- 2026-09-15T13:49Z First draft cleared the password at submit start; that would have flashed "Minimum 6 characters required" during the spinner because errors are derived live. Removed the clear (the state is dropped when the dialog unmounts).
- 2026-09-15T13:50Z Reworked `components/header/account-menu.tsx`: shows "Hello, <name>" / "<name> ›" when signed in; signed-out flyout's yellow Sign in button (and "New customer? Start here.") open the dialog; phone-width tap on the signed-out trigger opens the dialog directly; signed-in flyout shows "Signed in as <email>" and a "Sign Out" button in Your Account. Hover/click/Escape/outside-click behaviour kept; hover is ignored while the dialog is open; focus returns to the trigger when the dialog closes or on sign out.
- 2026-09-15T13:51Z `npx eslint lib/account.ts components/header/sign-in-dialog.tsx components/header/account-menu.tsx` → exit 0, no output.
- 2026-09-15T13:51Z `npx tsc --noEmit` → exit 0, no errors (whole project).
