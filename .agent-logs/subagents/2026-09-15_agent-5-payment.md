# Agent 5 — Payment card form

- **Scope:** Stripe Elements–style card form in checkout step 2.
- **Files owned:** `components/checkout/card-form.tsx` (new), `lib/card.ts` (new), `components/checkout/checkout-flow.tsx` (payment step only).

## Log

- 2026-09-15T13:46:49Z — Inspected `components/checkout/checkout-flow.tsx` (3-step flow, `PAYMENT` array with demo card + COD, `Step`/`Field` helpers) and `app/globals.css` tokens (`link`, `ease-amzn`, `field`, `btn-yellow`). Created this log.
- 2026-09-15T13:47:10Z — Created `lib/card.ts`: `detectBrand`, `formatCardNumber` (4-4-4-4 / Amex 4-6-5, truncates to 16/15), `formatExpiry` (`MM / YY`, auto `0` prefix for 2–9 and 13–19; separator only once a third digit exists so backspace never gets stuck), `parseExpiry`, `luhnValid`, `expiryValid(mm, yy, now)`, `cvcValid` (3, Amex 4), `BRAND_NAMES`, `onlyDigits`.
- 2026-09-15T13:47:40Z — Created `components/checkout/card-form.tsx`: Stripe-style single box (number row with live brand icon; expiry + masked CVC row with CVC icon), Cardholder name and ZIP fields, #0570de focus glow per segment (segments overlap by 1px and raise z-index on focus so the ring is never clipped), #df1b41 error state with AlertCircle message under the box. Validates on blur (non-empty fields), all fields on submit (focuses first invalid), clears an error as soon as the field turns valid. "Use test card" fills 4242…, 12/(year+3), 123, name/ZIP (keeps existing or uses `defaultName`/`defaultZip`). Lock-icon demo notice added. Only `{ brand, last4, name }` leave the component via `onSubmit` / `onValidChange`. Brand icons are hand-drawn inline SVGs (`BrandIcon` exported for the summary).
- 2026-09-15T13:48:00Z — First draft had a `useTestCard` handler (hooks lint would treat as a hook) and a manual separator-deletion branch in expiry onChange; renamed to `fillTestCard` and removed the branch (formatter change made it unnecessary) before linting.
- 2026-09-15T13:48:20Z — `checkout-flow.tsx` payment step: options are now "Credit or debit card" (expands inline `CardForm`) and "Pay on delivery". For card, "Use this payment method" is `type="submit" form="checkout-card-form"` so it runs the form's full validation and only advances on valid; COD advances directly. Collapsed summary shows `BrandIcon` + "Visa ending in 4242" or Banknote + "Pay on delivery". Added `(payment === "cod" || !!card)` to `canPlace`. Blue info banner, address step, delivery, totals, place order, redirect and empty-cart untouched. No payment field added to `Order`.
- 2026-09-15T13:48:40Z — `npx eslint components/checkout/card-form.tsx lib/card.ts components/checkout/checkout-flow.tsx` → exit 0, no output. `npx tsc --noEmit` → exit 0 (no errors in any file).
- 2026-09-15T13:49:00Z — Helper self-check: copied `lib/card.ts` to session scratchpad (outside repo), ran `node --experimental-strip-types check.ts` (Node 22.23.2). Results: luhn 4242424242424242=true, 4242424242424241=false; brand 4242=visa, 5105=mastercard, 2221=mastercard, 2720=mastercard, 2721=unknown, 3782=amex, 6011=unknown; formatCardNumber("4242424242424242999")="4242 4242 4242 4242", Amex "378282246310005123"="3782 822463 10005"; formatExpiry "0425"="04 / 25", "4"="04", "13"="01 / 3", "12"="12"; expiryValid (now 2026-09-15) 12/29=true, 09/26=true, 08/26=false, 13/30=false; cvcValid "123" amex=false, visa=true. All as expected.
- 2026-09-15T13:49:28Z — Re-ran eslint on checkout-flow and `npx tsc --noEmit` after the `canPlace` tweak → both exit 0.
