# Agent 3 — changeable delivery ZIP

- Scope: client-side persistent delivery ZIP; header Deliver-to button + phone strip + location modal; buy box label and date offset.
- Files owned: `lib/location.ts` (new), `components/header/deliver-to.tsx` (new), `components/header/header.tsx` (Deliver-to swap + phone strip only), `components/product/purchase.tsx` (label + date offset only).

## Log
- 2026-09-15T13:45:31Z Inspected `components/header/header.tsx` (hardcoded Deliver-to button at lg+, phone search row), `components/product/purchase.tsx` (`useClock`, two `deliveryDate(..., clock.now)` calls, hardcoded label), `lib/format.ts` `deliveryDate(days, from)`, and `app/globals.css` for `btn-yellow`, `btn-white`, `field`, `animate-fade-in` (150ms), `animate-rise` (200ms).
- 2026-09-15T13:45:50Z Created `lib/location.ts`: `useSyncExternalStore` over localStorage `amzn-clone:zip` (server snapshot "10001"), same-tab custom event + cross-tab `storage` event, try/catch around storage; `setDeliveryZip`, `isValidZip`, `zipLabel` prefix-range lookup, `zipDeliveryOffset` (0 for leading digit 0–2, else 1). No provider, so the root layout needs no changes.
- 2026-09-15T13:46:20Z Created `components/header/deliver-to.tsx`: `belt` (default) and `strip` variants sharing one "Choose your location" modal, rendered through a portal to body so the sticky header's stacking/text colour don't leak in. Digits-only 5-char input, red inline error (`text-deal` + `border-deal`), Apply saves and shows "Delivering to <label>" and closes after 900ms, Done/Escape/backdrop close, focus goes to the input on open and back to the trigger on close, Tab stays inside the dialog. Draft state is reset in the open handler rather than an effect, which keeps `react-hooks/set-state-in-effect` happy. Motion: animate-fade-in 150ms, animate-rise 200ms.
- 2026-09-15T13:46:40Z `header.tsx`: swapped the hardcoded button for `<DeliverTo className={beltLink + " hidden ... lg:flex"} />` and dropped the now-unused MapPin import; added a phone-only `<DeliverTo variant="strip" className="sm:hidden" />` under the phone search row.
- 2026-09-15T13:46:55Z `purchase.tsx`: `useDeliveryZip()` label replaces "New York 10001"; `zipOffset` added to both `deliveryDate(..., clock.now)` day counts. Nothing else touched. (First pass imported `zipLabel` separately; simplified to the hook's `label` instead.)
- 2026-09-15T13:47:13Z Verification: `npx eslint lib/location.ts components/header/deliver-to.tsx components/header/header.tsx components/product/purchase.tsx` → exit 0, no output. `npx tsc --noEmit` → exit 0, no errors in the project. Nothing failed.
